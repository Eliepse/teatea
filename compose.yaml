name: teatea

services:
  db:
    image: postgres:17-alpine
    restart: on-failure
    shm_size: 128mb
    environment:
      POSTGRES_DB: teatea
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "2345:5432"