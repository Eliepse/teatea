APP_ENV=dev
APP_SECRET=
DB_USER=admin
DB_PASSWORD=admin
DATABASE_URL="postgresql://admin:admin@db:5432/teatea?serverVersion=17&charset=utf8"

PUBLIC_API_URL=http://localhost/api

# Minimum delay a request must take.
# if the request is faster, a fake delay is added.
# - Only enabled in "dev" env
#PUBLIC_API_FAKE_DELAY=625
