info:
  name: Show
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost/api/posts/:id
  params:
    - name: id
      value: "10"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
