info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost/api/posts
  headers:
    - name: Content-Type
      value: application/ld+json
  body:
    type: json
    data: |-
      {
        "content": "{{$randomCatchPhrase}} {{$randomCatchPhrase}} {{$randomCatchPhrase}} {{$randomCatchPhrase}}. {{$randomCatchPhrase}} {{$randomCatchPhrase}} {{$randomCatchPhrase}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
